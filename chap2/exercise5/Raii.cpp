#include "Raii.h"

int main()
{
	/* this logic is not tested!!!
	db_manager db("172.17.42.1", "herbert", "NSA_go_away");
	auto rs = db.query("select problem from my_solutions "
			   "   where award_worthy != 0");
	while(rs->next())
		cout << rs->getString(1) << endl;
	*/
	return 0;
}
